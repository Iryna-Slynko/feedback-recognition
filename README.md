# feedback-recognition

## Server side

```powershell
$env:FLASK_APP = "server"
pip3 install
flask db migrate
flask run
```

Access: initial user password 'secret'

## API

```powershell
$env:FLASK_APP = "api"
flask run
```
